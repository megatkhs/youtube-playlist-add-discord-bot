# youtube-playlist-add-discord-bot

YouTubeの動画を再生リストに追加するDiscordボットです。

## 概要

このプロジェクトは、主に2つの部分で構成されています。

1. YouTubeのURLを含むメッセージを監視するDiscordボット。
2. YouTube APIのOAuth 2.0認証を処理するためのWebサーバー（Honoを使用）。

このボットは、Discordとの連携に`discord.js`、YouTube Data API v3との連携に`googleapis`、API認証情報の保存に`microCMS`を使用しています。

## コマンド

このボットにスラッシュコマンドはありません。ボットがアクセスできるチャンネルにYouTubeのURLを貼り付けるだけで、再生リストに追加されます。

## 実装の進め方

1. 実装計画を立てて GitHub に Issue を作成する
  - すでに存在する場合はそれに従う
  - **重要: プランモード（実装計画の提示中）において、ユーザーからの単なる質問やコメントを承認と見なさないこと。ユーザーから明示的な承認（「GO」「OK」など）を得るまでは、勝手に実装プロセス（プランモードからの離脱）に移行しないこと。**
2. 作業ブランチ`feature/{Issue番号}`を作成し実装を進める
3. 作業単位でコードをGitにステージする
  - その作業で編集したファイルのみをステージしてください
  - エージェントはコミットの直前に `npx difit` を実行しユーザーにレビューを依頼する
    - ユーザーの指示に応じて修正を行った場合は、**独断でコミットや次の手順に進まないこと**。再度確認を依頼し、明示的な承認を得てからコミットする。
4. プルリクエストを作成しレビューを依頼する
