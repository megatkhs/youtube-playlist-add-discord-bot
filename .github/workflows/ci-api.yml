name: CI (API)

on:
  push:
    branches-ignore:
      - main
    paths:
      - 'apps/api/**'
      - 'package.json'
      - 'bun.lockb'
  pull_request:
    paths:
      - 'apps/api/**'
      - 'package.json'
      - 'bun.lockb'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run API tests
        run: bun run --filter @yt-playlist-bot/api test
