{
  "name": "@yt-playlist-bot/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply youtube-playlist-bot --local",
    "db:migrate:remote": "wrangler d1 migrations apply youtube-playlist-bot --remote",
    "types": "wrangler types",
    "test": "vitest run"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.17"
  },
  "devDependencies": {
    "better-sqlite3": "^12.9.0",
    "dotenv": "^17.4.2",
    "drizzle-kit": "^0.31.10",
    "vitest": "^4.1.5",
    "wrangler": "^4.88.0"
  }
}
